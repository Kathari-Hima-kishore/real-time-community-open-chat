---
phase: 03-features
plan: '01'
type: execute
wave: 1
depends_on: [02-02]
files_modified: [index.html, main.js, style.css]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Messages persist in Firestore and load on page refresh"
    - "Each user has a colored avatar with initials"
    - "Header shows count of online users"
    - "Typing indicator shows when other users are typing"
  artifacts:
    - path: "index.html"
      contains: "typingIndicator, onlineCount"
    - path: "main.js"
      contains: "createAvatar, setupPresence, setTyping"
    - path: "style.css"
      contains: ".msg-avatar, .typing-indicator, .online-count"
---

<objective>
Add user avatars, online user count, and typing indicators.

Purpose: More engaging chat experience with visual presence.
</objective>

<context>
@.planning/ROADMAP.md
@index.html
@main.js
@style.css
</context>

<tasks>

<task type="auto">
  <name>Add user avatars</name>
  <files>main.js, style.css</files>
  <action>
    1. Add getAvatarColor() - generates consistent color from name
    2. Add getInitials() - extracts initials from display name
    3. Add createAvatar() - creates avatar DOM element
    4. Update renderMessages to include avatar next to each message
    5. Add CSS for .msg-avatar (32px circle, centered initials)
  </action>
  <verify>
    <manual>Open chat, verify each message has colored avatar with initials</manual>
  </verify>
  <done>Messages display user avatars with initials</done>
</task>

<task type="auto">
  <name>Add online user count</name>
  <files>index.html, main.js, style.css</files>
  <action>
    1. Add onlineCount span to header
    2. Add presence tracking using Firestore collection
    3. Use onSnapshot to count documents where online=true
    4. Display "1 user online" or "N users online"
    5. Add cleanup on page unload
  </verify>
  <done>Header shows real-time online user count</done>
</task>

<task type="auto">
  <name>Add typing indicators</name>
  <files>index.html, main.js, style.css</files>
  <action>
    1. Add typingIndicator div above messages
    2. Create typing collection in Firestore
    3. On input, set typing document (auto-deletes after 3s)
    4. Listen to typing collection, show "X is typing..." or "N people are typing..."
    5. Clear indicator when user sends message
  </action>
  <verify>
    <manual>Open two browser tabs, type in one, verify typing indicator shows in other</manual>
  </verify>
  <done>Typing indicator shows when other users are typing</done>
</task>

</tasks>

<verification>
- [ ] Avatars display with colored background and initials
- [ ] Online count updates when users join/leave
- [ ] Typing indicator appears in other users' windows
</verification>

<success_criteria>
User avatars with initials, online user count in header, typing indicators showing who is typing.
</success_criteria>

<output>
After completion, create `.planning/phases/03-features/03-01-SUMMARY.md`
