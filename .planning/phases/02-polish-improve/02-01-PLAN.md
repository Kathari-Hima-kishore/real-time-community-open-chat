---
phase: 02-polish-improve
plan: '01'
type: execute
wave: 1
depends_on: []
files_modified: [index.html, main.js, style.css]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Users are prompted for display name on first visit"
    - "Users can change their display name anytime via settings"
    - "Messages display with user's chosen name instead of random ID"
  artifacts:
    - path: "index.html"
      provides: "Settings icon in header for name change"
      contains: "settings icon button"
    - path: "main.js"
      provides: "Display name management logic"
      contains: "localStorage displayName"
    - path: "style.css"
      provides: "Modal and settings styling"
      contains: "modal styles"
---

<objective>
Add user display names to the chat with first-visit prompt and settings change capability.

Purpose: Replace cryptic user IDs with friendly names for better UX.
Output: Users see and control their display name in the chat.
</objective>

<execution_context>
@C:/Users/Hima Kishore/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-polish-improve/02-CONTEXT.md
@index.html
@main.js
@style.css
</context>

<tasks>

<task type="auto">
  <name>Add display name modal and localStorage management</name>
  <files>main.js</files>
  <action>
    In main.js, add displayName management:
    1. Check localStorage for 'displayName' on load
    2. If missing, show modal prompt for name entry
    3. Validate name (non-empty, max 20 chars)
    4. Save to localStorage on submit
    5. Export getDisplayName() function for message rendering

    Use existing IIFE pattern. Keep inline - no separate modal HTML needed, use prompt() or create simple inline modal.
  </action>
  <verify>
    <automated>Manual: Open index.html in browser, clear localStorage, refresh - should see name prompt</automated>
    <manual>Verify name persists after page refresh, verify empty/invalid names are rejected</manual>
  </verify>
  <done>Users prompted for name on first visit, name saved to localStorage</done>
</task>

<task type="auto">
  <name>Add settings icon and name change functionality</name>
  <files>index.html, main.js, style.css</files>
  <action>
    1. In index.html: Add settings icon/button to header (use Unicode gear ⚙ or similar)
    2. In main.js: Add click handler that prompts for new name and updates localStorage
    3. In style.css: Add minimal styles for header layout adjustments if needed

    The settings click should prompt for new name using same validation as initial prompt.
  </action>
  <verify>
    <manual>Click settings icon, enter new name, verify messages now show new name</manual>
  </verify>
  <done>Users can change their display name anytime via settings icon</done>
</task>

<task type="auto">
  <name>Update message rendering to use display name</name>
  <files>main.js</files>
  <action>
    In renderMessages function:
    1. Replace `m.from` (userId) with display name
    2. For messages from OTHER users, store/retrieve their display name from localStorage or use a hash of their userId as fallback display name
    3. Display format: "{displayName}: {message}"

    Note: For simplicity, generate a deterministic "display name" for other users based on their userId (e.g., "User ABC") since there's no server-side user database.
  </action>
  <verify>
    <manual>Send a message, verify it shows your display name. Open in another browser/incognito, set different name, verify each user sees their own name</manual>
  </verify>
  <done>Messages display with user's chosen name instead of random ID</done>
</task>

</tasks>

<verification>
- [ ] First visit shows name prompt
- [ ] Name persists in localStorage across refresh
- [ ] Settings icon allows name change
- [ ] Own messages show my display name
  - Other users' messages show their display name (or generated fallback)
</verification>

<success_criteria>
Display name prompt on first visit, settings icon to change name, messages show display name instead of userId.
</success_criteria>

<output>
After completion, create `.planning/phases/02-polish-improve/02-01-SUMMARY.md`
</output>
