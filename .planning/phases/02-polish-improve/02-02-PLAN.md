---
phase: 02-polish-improve
plan: '02'
type: execute
wave: 2
depends_on: [02-01]
files_modified: [main.js, style.css]
autonomous: true
requirements: []
user_setup: []

must_haves:
  truths:
    - "Messages support basic markdown (bold, italic, links, inline code)"
    - "URLs are automatically detected and rendered as clickable links"
    - "Timestamps shown on messages (relative for <24h, absolute for >24h)"
    - "User input is sanitized - HTML stripped, markdown rendered safely"
  artifacts:
    - path: "main.js"
      provides: "Markdown parsing, URL detection, timestamp formatting, XSS sanitization"
      contains: "sanitize, parseMarkdown, formatTimestamp"
    - path: "style.css"
      provides: "Styles for links, timestamps, formatted content"
      contains: ".msg a, .msg .timestamp, .msg strong, .msg em"
---

<objective>
Add message formatting (markdown), timestamps, and XSS sanitization.

Purpose: Richer messages with safe HTML rendering and time context.
Output: Messages display with formatting, timestamps, and safe HTML.
</objective>

<execution_context>
@C:/Users/Hima Kishore/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/phases/02-polish-improve/02-CONTEXT.md
@.planning/phases/02-polish-improve/02-01-PLAN.md
@main.js
@style.css
</context>

<tasks>

<task type="auto">
  <name>Add input sanitization (XSS prevention)</name>
  <files>main.js</files>
  <action>
    Add sanitization function that:
    1. Strips ALL HTML tags using regex (remove <[^>]*> patterns)
    2. Escapes HTML entities (&, <, >, ", ') for safety
    3. Apply sanitization to user input before sending to Firestore

    This prevents XSS while allowing safe text display.
  </action>
  <verify>
    <manual>Try sending message with &lt;script&gt;alert('xss')&lt;/script&gt; - should display as plain text, not execute</manual>
  </verify>
  <done>All user input is sanitized - HTML stripped, special characters escaped</done>
</task>

<task type="auto">
  <name>Add basic markdown parsing</name>
  <files>main.js, style.css</files>
  <action>
    Add parseMarkdown function supporting:
    1. **bold** - **text** or __text__
    2. *italic* - *text* or _text_
    3. `inline code` - backtick wrapped
    4. [links](url) - markdown style links

    Use innerHTML injection AFTER sanitization (sanitize first, then apply markdown).
    Add minimal CSS for .msg strong, .msg em, .msg a.
  </action>
  <verify>
    <manual>Send "This is **bold** and *italic* with `code`" - verify formatting renders. Send "[click me](https://example.com)" - verify link is clickable.</manual>
  </verify>
  <done>Messages support bold, italic, inline code, and markdown links</done>
</task>

<task type="auto">
  <name>Add URL auto-detection and linking</name>
  <files>main.js</files>
  <action>
    Extend the markdown/link parsing to auto-detect raw URLs:
    1. Regex find URLs (http://, https://)
    2. Convert to clickable links automatically
    3. Apply after markdown parsing

    Example: "Check out https://example.com" becomes "Check out &lt;a href='https://example.com'&gt;https://example.com&lt;/a&gt;"
  </action>
  <verify>
    <manual>Send "Visit https://www.google.com without markdown" - verify URL becomes clickable</manual>
  </verify>
  <done>Raw URLs are automatically converted to clickable links</done>
</task>

<task type="auto">
  <name>Add timestamps to messages</name>
  <files>main.js, style.css</files>
  <action>
    Add timestamp display to each message:
    1. Show timestamp next to or below author name
    2. For messages &lt; 24h old: show relative time ("5 min ago", "2 hours ago")
    3. For messages &gt; 24h old: show absolute time ("Jan 15, 10:30 AM")
    4. Update relative times dynamically (re-render or updateInterval)

    Use setInterval to refresh relative timestamps every 60 seconds.
    Add CSS for .msg .timestamp with smaller, muted styling.
  </action>
  <verify>
    <manual>Send message, verify timestamp shows "just now" or "X min ago". Wait 2 minutes, verify it updates. Check older messages show absolute date.</manual>
  </verify>
  <done>Timestamps shown on messages with hybrid relative/absolute display</done>
</task>

</tasks>

<verification>
- [ ] XSS test: HTML in messages renders as plain text
- [ ] Markdown: **bold**, *italic*, `code`, [links](url) render correctly
- [ ] Auto-links: https:// URLs become clickable
- [ ] Timestamps: Relative time for recent, absolute for old
- [ ] Timestamp updates: Relative times refresh automatically
</verification>

<success_criteria>
Messages display with markdown formatting, timestamps, and safe HTML. XSS attempts are neutralized.
</success_criteria>

<output>
After completion, create `.planning/phases/02-polish-improve/02-02-SUMMARY.md`
</output>
